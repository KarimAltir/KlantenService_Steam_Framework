@model IEnumerable<KlantenService_Steam_Framework.Models.Game>

@{
    ViewData["Title"] = "Games";
}

<h1>Games</h1>

@* <p>
    <a asp-action="Create">Create New</a>
</p> *@
<table class="table">
    <thead>
        <tr>
            <th>
                Games
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Games_count)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
                <img src="@item.Background_image" alt="background image" height="180px"/>
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Games_count)
            </td>
@*             <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td> *@
        </tr>
} 
    </tbody>
</table>
